define(["jquery",'Magento_ConfigurableProduct/js/configurable'],function($){'use strict';$.widget('diptyque.configurable',$.mage.configurable,{_configureElement:function(element){this._super(element);var isAvailable=true;if(element.value){isAvailable=element.selectedOptions[0].config.isAvailable;}
this._addToCartStatus(isAvailable);$(document).trigger('configurable:newElementSelected',element.selectedOptions[0].config);},_addToCartStatus:function(isAvailable){if(isAvailable){$('.box-tocart').show();}else{$('.box-tocart').hide();}}});return $.diptyque.configurable;});